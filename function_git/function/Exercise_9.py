#'Write a function is_member() that takes a value (i.e. a number, string, etc) x and a list of values a,
def is_member(x, a):
    if x in a:
        print (True)
    else:
        print (False)
        
      
is_member('hello',['a',1,2,3,'b','hello'])
